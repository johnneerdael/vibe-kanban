services:
  vibe-kanban:
    container-name: "vibe-kanban"
    image: johnneerdael/vibe-kanban:main
    ports:
      - "3000:3000"
    volumes:
      - ./data:/data
      - ./repos:/repos
    env_file:
      - .env
    restart: unless-stopped
